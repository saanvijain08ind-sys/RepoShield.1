# Groq API key: powers AI remediation & outreach generation (llama-3.1-8b-instant, llama-3.3-70b-versatile).
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=""

# GitHub personal access token (classic PAT with `repo` scope): enables the automated
# fork-and-PR remediation pipeline (branch `reposhield/fix-<cve-id>` + pull request).
GITHUB_TOKEN=""

# Node environment
NODE_ENV="development"
